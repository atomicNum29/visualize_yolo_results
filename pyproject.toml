[project]
name = "visualize-yolo-results"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "duckdb>=1.4.3",
    "fastapi>=0.128.0",
    "pyarrow>=23.0.0",
    "uvicorn>=0.40.0",
]
